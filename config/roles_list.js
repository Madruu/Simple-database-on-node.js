//Initiating with only 3 simple user roles
//The roles will be stored inside an object and each role will have its own "number" identifications
const ROLES_LIST = {
    "Admin": 5150,
    "Editor": 1984,
    "User": 2001
}

export default ROLES_LIST;